#!/usr/bin/env bash
# Compatibility entry point; dependency installation is explicit.
set -euo pipefail
if [[ "${1:-}" == "--no-run" ]]; then
  exec "$(dirname "$0")/setup"
fi
exec "$(dirname "$0")/develop" "$@"
